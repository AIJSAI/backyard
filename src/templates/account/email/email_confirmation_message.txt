{% extends "account/email/base_message.txt" %}
{% load i18n %}
{% comment %} PROJECT OVERRIDE. The stock body read "Hello from backyard.family! You're
   receiving this email because user james has given your email address to register an
   account on backyard.family. ... Thank you for using backyard.family!" — a username, a
   bare domain three times, "register an account", and two exclamation marks, in a message
   a relative reads on their phone. It says what the address is FOR and nothing else, and
   it never prints a member's username.

   THE SECOND SENTENCE CHANGED ON 2026-09-19 (walk item 24 review). It used to promise
   "Nothing else is sent to this address unless you ask for it", and one tap on this link
   can now also start the Family email — for the member's PRIMARY sign-in address, where
   they were asked. A mail that understates what its own link does is the kind of consent
   defect nobody notices until content arrives somewhere it was not expected, so the mail
   names both outcomes. A SECONDARY address is not collapsed at all and still gets the
   digest's own content-free confirmation (core/digesting.py `_own_signin_address`), which
   is where that question is asked. {% endcomment %}
{% block content %}{% autoescape off %}{% blocktrans %}Someone added this address to their family's Backyard.

If that was you, confirm it here:{% endblocktrans %}

{% if code %}{{ code }}{% else %}{{ activate_url }}{% endif %}

{% blocktrans %}Confirming means we can send you a way back in if you ever forget your password, and it starts the Family email if one has been pointed at this address.

If it was not you, ignore this email. Nothing else will be sent here.{% endblocktrans %}{% endautoescape %}{% endblock content %}
